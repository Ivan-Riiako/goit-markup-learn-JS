const Transaction = {
    DEPOSIT: 'deposit',
    WITHDRAW: 'withdraw',
};

const account = {
    balance: 0,
    transaction: [],
    createTransaction(amount, type) {
        const transaction = {
        amount,
        type,
        date=
        };
    }
}